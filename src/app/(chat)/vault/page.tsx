export default function VaultPage() {
  return (
    <main className="h-screen bg-[var(--bg)] px-6 py-8">
      <h1 className="text-lg font-semibold text-[var(--text)]">Vault</h1>
      <p className="mt-1 text-[13px] text-[var(--text-muted)]">
        Store and organize local legal documents.
      </p>
    </main>
  );
}
